RSpec.describe Google::Auth::Stores::AwsParameterStore do
  it "has a version number" do
    expect(Google::Auth::Stores::AwsParameterStore::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
